# 开发计划：AI 命理伴侣 Web MVP

> 从 0 到上线，分阶段交付，每阶段可测试可验证
> 技术栈: Next.js + Node.js + SQLite(dev) + Claude API
> 当前状态: Phase 0-5 完成 + UI 新中式禅意翻新 + 每日灵签上线 + 管理后台上线

---

## 总体阶段划分

```
Phase 0: 基础设施搭建 (3-5 天)
Phase 1: 每日运势 (5-7 天)     ← 最快出效果，验证核心价值
Phase 2: AI 占卜 (7-10 天)     ← 核心功能，验证"准"度
Phase 3: AI 对话 (5-7 天)      ← 留存功能，验证粘性
Phase 4: 命书 + 付费 (7-10 天)  ← 变现功能，验证商业模式
Phase 5: 联调 + 上线 (5-7 天)
────────────────────────────────
总计: 约 5-7 周
```

---

## Phase 0: 基础设施搭建

### 目标
- 前后端项目能跑起来
- Claude API 连通
- 基础用户系统跑通
- 能部署到公网验证

### 任务清单

#### 0.1 前端初始化

```
创建 Next.js 项目
  npx create-next-app@latest web --typescript --tailwind --app

目录结构:
  web/
  ├── app/
  │   ├── layout.tsx          # 全局布局 (主题/字体) — 新中式禅意，暖色背景
  │   ├── page.tsx            # 首页 (运势+灵签入口) — 评分/分段/宜忌/灵签卡片
  │   ├── login/page.tsx      # 登录 — 龙纹徽章+禅意输入框+出生时辰下拉
  │   ├── divination/page.tsx # 占卜 — 底部导航整合，禅意风格
  │   ├── chat/page.tsx       # AI 对话 — 底部导航整合
  │   ├── daily-sign/page.tsx # 每日灵签 — 花窗+签语+四段解读+时辰指引
  │   ├── fate-book/page.tsx  # 命书 — 底部导航整合
  │   └── profile/page.tsx    # 我的 — 退出登录，底部导航
  ├── components/              # 复用组件
  ├── lib/                     # API 客户端/工具函数
  └── public/                  # 静态资源

UI 框架:
  选型: Tailwind CSS + shadcn/ui (按钮/卡片/输入框等)
  理由: 快速出 UI，不需要设计师也能做得好
    
  新中式禅意主题:
  · 宣纸暖白底色: #f5f0e8
  · 卡片表面: #fefcf9 / #eee6dc 边框
  · 主色(铜): #c4835a / 金色: #c4a04c
  · 辅助色: #7a9e7a / #c45a4a / #7a8ea8
  · 毛玻璃效果: backdrop-blur-xl + 半透明背景
  · 右侧暖色渐变氛围光

包管理: 用 yarn (npm 在 npmmirror 上频繁超时)
```

#### 0.2 后端确认

```
现有后端确认/调整:

  ├── 数据库: SQLite (开发用) / PostgreSQL (生产)
  │   ├── 开发阶段用 SQLite，零配置直接 `prisma db push`
  │   └── 生产环境切 PostgreSQL，修改 schema 中 provider 即可
  ├── 端口: 后端 3001，前端 3000 (避免冲突)
  ├── API 路由: auth/readings/chat/user 全部可用
  ├── Claude API: 缺少 API Key 时有本地运势兜底算法
  ├── 包管理: 用 yarn (npm 在 npmmirror 上超时)
  └── 部署: 用 Vercel (前端) + Railway/Fly.io (后端)？

注意: SQLite 不支持原生 Json 和 String[] 类型
  → 所有 Json/String[] 字段改为 String，存储 JSON.stringify 的字符串
  → 读取时 JSON.parse 还原，API 返回时自动解析
```

#### 0.3 用户系统

```
功能:
  ├── 昵称注册登录 (JWT)
  ├── 出生信息录入 (日期+时间(12时辰下拉)+出生地)
  │   └── 时辰: 子丑寅卯辰巳午未申酉戌亥 十二时辰，可选"不知道"
  │   └── 出生地: 文本输入，选填
  ├── 星座/生肖自动计算 (注册时通过生日计算)
  └── Session 管理 (token 持久化到 localStorage)

测试方法:
  ├── 注册流程走通 (含时辰+出生地)
  ├── 登录后 token 有效
  ├── 出生信息修改后星座重新计算
  └── 退出登录后无法访问需要认证的接口
```

#### 0.4 部署

```
前端 → Vercel (免费)
  └── 自动部署 GitHub 主分支

后端 → Railway / Fly.io (按量计费)
  └── PostgreSQL 托管
  └── Docker 部署

域名 → 可选 (先用 Vercel 提供的域名)
```

### 可交付物

```
✅ 浏览器打开能看到登录/注册页面
✅ 输入出生信息后能看到星座信息
✅ Claude API 能在后端正常调用
✅ 前后端能通信
✅ 部署后公网可访问
```

### Phase 0 追加: UI 全面翻新 + 灵签 (v0.3)

```
UI 翻新内容:
  ├── 全局 CSS 重写: 暗色主题 → 新中式禅意 (暖白宣纸底)
  ├── 毛玻璃效果: 所有卡片 bg-surface/80 + backdrop-blur-xl
  ├── 右侧氛围光: body::before radial-gradient blur
  ├── 底部导航: 5 标签 (首页/占卜/对话/命书/我的)
  │   └── bg-surface/85 + backdrop-blur-xl + SVG 线框图标
  ├── 中文字体栈: PingFang SC → Hiragino Sans GB → Microsoft YaHei
  └── 字体粗细: body 使用 font-normal (400)，大标题用 font-thin (100)

新组件:
  ├── ChineseWindow ← 四叶花窗 SVG + 水墨山水画 (远山/亭台/松/舟/鸟/桃花)
  ├── DragonLogo   ← 圆形龙纹徽章，用于登录页和我的页面
  └── BottomNav    ← 5 标签固定底栏，毛玻璃效果，激活指示器

新页面:
  ├── /daily-sign  ← 每日灵签完整页 (花窗+签到+四段命理解读+时辰指引)
  └── /profile     ← 我的 (退出登录)

灵签功能 — 后端:
  ├── aiService.ts 新增:
  │   ├── 天干地支计算: getDayGanzhi(), getMonthGanzhi()
  │   ├── 五行系统: STEM_ELEMENT / BRANCH_ELEMENT 映射表
  │   ├── 生克关系: getElementRelation() → same/generates_me/i_generate/controls_me/i_control
  │   ├── 签语模板: SIGN_BY_RELATION (5 关系 × 2 变体)
  │   ├── 四段解读: CAREER/WEALTH/LOVE/HEALTH_READINGS (5 关系 × 2 变体 × 4 维度 = 40)
  │   ├── 吉时推荐: getTimeSlotForElement() 基于五行相生
  │   └── generateDivineSign(): 日主计算 → 关系判断 → 签语+解读+指引
  ├── readings.ts 更新:
  │   ├── formatFortune() 解析 divineSign JSON
  │   └── 自动补丁: 旧缓存记录缺失 divineSign 时即时生成
  └── schema.prisma: DailyFortune 新增 divineSign String? 字段

灵签功能 — 前端:
  ├── daily-sign/page.tsx: 全状态 (loading/empty/data)
  ├── 加载态: 水墨太极动画 + "抽取灵签中"
  ├── 空态/错误态: 重新加载按钮 + 返回首页
  ├── 数据态: 返回按钮 → 干支日期 → 花窗 → 签语 → 基调色 → 日主分析 → 四段解读卡片 → 行动指引+时辰 → 品牌尾
  └── api.ts: fortune.daily() 返回类型扩展 divineSign 字段

注意点:
  ├── 字体避免 font-light (用户反馈"字看不清")
  ├── 灵签基于真实历法而非随机: 同一用户同一天结果一致
  └── 旧缓存兼容: 自动补丁 + 需要时手动清库 (pm prisma db push --accept-data-loss)
```

---

## Phase 1: 每日运势

### 目标
- 用户每天早上能看到今日运势
- 运势内容有依据，不是随机生成
- 用户能反馈"准"或"不准"

### 功能详情

```
1.1 后端 - 运势生成

流程:
  ├── 首次访问时实时生成
  ├── 基于: 星座 + 生肖 + 出生时辰 + 出生地 + 当日地支 (十二时辰)
  ├── 输出: 综合评分 + 分段评分 + 文案 + 幸运信息 + 宜/忌(基于当日地支)
  └── 缓存: 同一天同一用户返回相同数据 (不重复计算)

宜/忌动态生成:
  ├── 计算当日地支 (基于日期，参考 2000-01-01 甲子日)
  ├── 每个地支有专门的宜/忌对照表 (基于黄历传统)
  ├── 根据用户的出生时辰和生肖进行个性化轮转
  └── 格式: "酉日宜 · 祭祀祈福"

API:
  GET /api/v1/readings/daily-fortune/:userId
  → 返回: 评分/分段/文案/幸运信息/宜(3条)/忌(3条)/星座/生肖

1.2 前端 - 首页展示

布局:
  ┌─────────────────┐
  │ 早安，小曼 ✨    │
  │ 5月18日 星期日   │
  │                 │
  │  ┌───────────┐  │
  │  │ 综合运势   │  │
  │  │    78     │  │
  │  │  评分不错  │  │
  │  └───────────┘  │
  │                 │
  │ 爱情 82 事业 65  │
  │ 财运 70 健康 75  │
  │                 │
  │ ┌──────┬──────┐ │
  │ │ 宜   │ 忌   │ │
  │ │ 祭祀 │ 嫁娶 │ │
  │ │ 祈福 │ 出行 │ │
  │ └──────┴──────┘ │
  │                 │
  │ 幸运色: 深蓝    │
  │ 幸运数字: 7     │
  │ 幸运方向: 东南  │
  │                 │
  │ ┌─────────────┐ │
  │ │  今日灵签    │ │
  │ │ 厚土载物    │ │
  │ │ 守静笃      │ │
  │ │  [查看完整]  │ │
  │ └─────────────┘ │
  │                 │
  │ ▼ 查看详情      │
  └─────────────────┘

交互:
  ├── 打开首页自动显示当日运势
  ├── 分段评分用进度条显示
  ├── 点击"详情"展开 AI 运势文案
  ├── 灵签卡片点击进入 /daily-sign 完整页
  └── 底部"准" / "不准" 按钮

文案策略:
  ├── 默认显示 2-3 句 (免费)
  ├── 点击"查看完整" → 弹出登录/付费
  └── 初期先全部免费，测试"准"率
```

### 测试方法

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 内容准确性 | 人工评审 | 3 人以上认为"符合当天实际" |
| 同星座一致性 | 同一星座用户对比 | 核心结论一致，细节不同 |
| 个性化程度 | 不同星座对比 | 内容有星座差异，不是套模板 |
| 文案质量 | 用户反馈收集 | 前 100 个用户"准"率 ≥ 60% |
| 性能 | 100 并发请求 | P95 响应 < 2s |
| 缓存 | 同一用户重复请求 | 返回相同数据 (非重复计算) |

**验收标准：**
```
1. 每天打开首页显示今日运势 ✓
2. 12 星座内容不同 ✓
3. 同星座用户连续 3 天每天不同 ✓
4. 用户可反馈"准" / "不准" ✓
```

---

## Phase 2: AI 占卜

### 目标
- 用户能提问并获得占卜解读
- 塔罗抽牌流程有仪式感
- 解读有内容深度，不是套话

### 功能详情

```
2.1 后端 - 占卜服务

塔罗知识库:
  ├── 78 张韦特塔罗牌 (正位+逆位)
  ├── 每张牌: 关键字/感情解读/事业解读/财运解读/象征意义
  ├── 牌阵: 单牌/三牌(过去-现在-未来)
  └── 存储: 向量库 (PGVector) + JSON

API:
  POST /api/v1/readings/tarot
  → 参数: question, spreadId, userId
  → 返回: 牌面 + 简版解读 (免费) / 详版解读 (付费)

2.2 前端 - 占卜页面

交互流程:
  ┌──────────────────────────┐
  │ 有什么想问命运的吗？      │
  │                          │
  │ [___________________]    │
  │                          │
  │ 他喜欢我吗？             │
  │ 我该换工作吗？           │
  │ 最近财运如何？           │
  │                          │
  │ [选牌阵] ▼               │
  │  单牌指引 三牌展开       │
  │                          │
  │ [开始占卜]               │
  │                          │
  │ ── 结果 ──              │
  │ 🃏 🃏 🃏                │
  │ (翻牌动画)              │
  │                          │
  │ AI 解读                  │
  │ (简版 3-5 句)            │
  │                          │
  │ [查看完整解读 ¥6]        │
  └──────────────────────────┘

免费 vs 付费:
  免费:
  ├── 每日 2 次
  ├── 显示 3 张牌面 + 每张牌 1 句解读
  └── 综合建议 1 句

  付费:
  ├── 无限次
  ├── 每张牌详细解读 (100+ 字)
  ├── 牌面组合分析
  ├── 综合建议 (200+ 字)
  └── 可保存/分享

翻牌动画:
  ├── 牌背显示 → 点击翻牌 → 翻转动画 → 显示牌面
  ├── 使用 CSS transform 3D 翻转
  └── 三张牌依次翻转 (间隔 0.5s)
```

### 测试方法

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 随机性 | 抽牌 1000 次统计分布 | 78 张牌出现概率均匀 (各 ~1.3%) |
| 逆位概率 | 统计逆位比例 | 约 30% (可配置) |
| 解读相关性 | 人工评审 | 解读内容和问题相关，不是通用回复 |
| 牌意准确性 | 对照经典塔罗书籍 | 不违背传统牌意 |
| 免费→付费转化 | A/B 测试免费内容量 | 付费转化率 ≥ 5% |
| 页面加载 | Lighthouse | Performance ≥ 80 |

**验收标准：**
```
1. 输入问题 → 抽牌 → 显示结果，完整流程走通 ✓
2. 翻牌动画流畅 ✓
3. 解读内容看起来"有道理" ✓
4. 付费后可查看完整解读 ✓
5. 历史记录可查看之前的占卜 ✓
```

---

## Phase 3: AI 对话

### 目标
- 用户可以和 AI 命理师自由对话
- AI 记住之前的对话和占卜记录
- 对话自然，不像在和机器人聊天

### 功能详情

```
3.1 后端 - 对话服务

记忆机制:
  ├── 当前会话: 最近 20 轮 (Redis)
  ├── 用户摘要: 星座/最近关注话题 (PostgreSQL)
  ├── 历史关联: 最近的占卜记录 (按需加载)
  └── 过期: 对话超过 7 天自动归档

API:
  POST /api/v1/ai/chat
  → 参数: message, userId, contextId?
  → 返回: reply, contextId

3.2 前端 - 对话页面

交互流程:
  ┌──────────────────────────┐
  │ 💬 星命                   │
  │                          │
  │ 用户: 他三天没找我了      │
  │                          │
  │ AI: 我看看你上次的占卜    │
  │ 记录...你抽到了星星牌    │
  │ 正位，暗示新的开始。    │
  │ 三天不联系不一定代表     │
  │ 什么哦 ✨                │
  │                          │
  │   [想听听塔罗的看法吗？] │
  │                          │
  │ [____________________] 📤│
  └──────────────────────────┘

特点:
  ├── 对话气泡 UI (类似微信)
  ├── AI 回复可包含占卜引导按钮
  ├── 显示 AI "正在输入..." 动画
  └── 免费版每日 10 条对话

免费 vs 付费:
  免费:
  ├── 每日 10 条
  ├── AI 回复 2-3 句
  └── 回复结尾附带付费引导

  付费:
  ├── 无限对话
  ├── AI 回复完整 (4-6 句)
  └── 可主动要求占卜
```

### 测试方法

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 上下文记忆 | 连续问 5 个相关问题 | AI 记得前面的内容，回答不矛盾 |
| 角色一致性 | 同一问题反复问 | 回答风格一致，不矛盾 |
| 占卜关联 | 先占卜再问相关问题 | AI 提到之前的占卜结果 |
| 对话自然度 | 真人盲测 | 30% 以上的人认为像真人对话 |
| 轮次深度 | 记录平均对话轮次 | 平均 ≥ 5 轮/次 |
| 用户留存 | 对话用户次日回访率 | ≥ 50% 对话用户次日回来 |

**验收标准：**
```
1. 可以和 AI 连续对话 10 轮以上 ✓
2. AI 记得对话前 5 轮的内容 ✓
3. AI 知道用户上次占卜了什么 ✓
4. 对话风格一致，不跳戏 ✓
5. 免费→付费的引导自然不烦人 ✓
```

---

## Phase 4: 命书 + 付费

### 目标
- 用户能购买深度命理报告
- 免费试读能吸引用户付费
- 付费流程流畅

### 功能详情

```
4.1 后端 - 命书服务

API:
  POST /api/v1/fatebook/generate  → 生成命书
  GET  /api/v1/fatebook/:userId   → 获取命书
  GET  /api/v1/fatebook/preview   → 免费试读

命书结构:
  ├── 性格特质 (免费试读 3 条)
  ├── 感情模式 (付费)
  ├── 事业发展 (付费)
  ├── 财运趋势 (付费)
  └── 月度更新 (购买后每月更新)

4.2 前端 - 命书页面

交互流程:
  ┌──────────────────────────┐
  │ 📖 你的命书               │
  │                          │
  │ 你的性格关键词            │
  │ 独立、敏感、有创造力      │
  │ (免费 3 条)              │
  │                          │
  │ ░░░░░░░░░░░░░░░░░░░░    │
  │ (模糊遮挡的部分)          │
  │                          │
  │ [查看完整命书 ¥38]       │
  │                          │
  │ 已解锁: 性格 3/10        │
  │ 未解锁: 感情/事业/财运   │
  └──────────────────────────┘

支付:
  ├── 接入 Stripe (海外) / 微信支付 (国内)
  ├── 购买前先展示价值 (试读+用户评价)
  └── 支付后立即解锁，无需等待

月度更新:
  ├── 购买命书的用户每月 1 日收到更新
  ├── 推送/邮件通知
  └── 更新内容: 当月运势 + 重点提醒
```

### 测试方法

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| 试读吸引力 | A/B 测试试读条数 | 3 条 vs 5 条，哪个转化高 |
| 支付流程 | 完整走通支付 | 支付成功→内容解锁≤5s |
| 内容质量 | 付费用户满意度调查 | 满意度 ≥ 4/5 |
| 月度更新 | 检查更新是否准时 | 每月 1 日自动生成 |
| 退款率 | 监控 | 退款率 < 3% |
| 用户评价 | 购买后引导评价 | 收集 20+ 条有效反馈 |

**验收标准：**
```
1. 免费试读 3 条 + 模糊遮挡效果 ✓
2. 支付后完整命书可查看 ✓
3. 命书内容看起来专业有个性化 ✓
4. 购买后次月收到更新推送 ✓
5. 支付流程 ≤ 3 步 ✓
```

---

## Phase 5: 联调 + 上线

### 目标
- 所有功能联调通过
- 性能达标
- 上线准备完成

### 任务清单

```
5.1 全功能联调
  ├── 注册→登录→首页运势→占卜→对话→命书→支付
  ├── 边界情况 (未登录/免费用完/网络差)
  └── 数据一致性 (占卜后历史可查)

5.2 性能优化
  ├── 首屏加载 < 3s (Vercel + CDN)
  ├── API 响应 < 1s (缓存策略)
  ├── 图片/资源优化
  └── Lighthouse 评分 ≥ 80

5.3 数据分析接入
  ├── 用户行为埋点 (PostHog / Amplitude)
  ├── 核心指标看板 (DAU/留存/付费转化)
  └── 告警机制 (接口错误率 > 5%)

5.4 上线清单
  ├── 隐私政策 + 用户协议
  ├── 合规标注 ("仅供娱乐参考")
  ├── 错误页面 (404/500/限流)
  ├── SEO 基础 (title/description/og)
  └── 分享卡片生成 (社交裂变基础)
```

---

## 整体时间线

```
Week 1:  Phase 0 (基础设施)
  Mon-Tue: Next.js 初始化 + Tailwind + shadcn/ui
  Wed-Thu: 后端确认 + 数据库就绪
  Fri:     Claude API 连通 + 用户系统
  Sat-Sun: 部署到 Vercel + Railway

Week 2:  Phase 1 (每日运势)
  Mon-Tue: 后端运势生成
  Wed-Fri: 前端运势页面 + 展示
  Sat-Sun: 运势质量测试 + 调优

Week 3:  Phase 2 (AI 占卜)
  Mon-Wed: 塔罗知识库 + 抽牌算法
  Thu-Fri: 前端占卜页面 + 翻牌动画
  Sat-Sun: 解读质量测试 + 调优

Week 4:  Phase 2 (继续) + Phase 3 (AI 对话)
  Mon-Tue: 付费版占卜 + 历史记录
  Wed-Fri: AI 对话后端 + 记忆系统
  Sat-Sun: 对话前端 + 测试

Week 5:  Phase 4 (命书 + 付费)
  Mon-Wed: 命书生成 + 免费试读
  Thu-Fri: 支付接入 + 前端
  Sat-Sun: 命书质量测试

Week 6:  Phase 5 (联调 + 上线)
  Mon-Wed: 全功能联调
  Thu-Fri: 性能优化 + 数据埋点
  Sat-Sun: 上线准备 + 部署
```

---

## 每个阶段的 Go/No-Go 决策点

```
Phase 0 → Phase 1: 前后端通信正常，用户系统可用？
  NO → 回到 Phase 0 修补基础设施
  OK → 进入每日运势开发

Phase 1 → Phase 2: 运势内容用户觉得"准"？
  NO → 调 prompt / 调整数据源
  OK → 进入 AI 占卜开发

Phase 2 → Phase 3: 占卜解读质量达标？
  NO → 补充知识库 / 优化 RAG
  OK → 进入 AI 对话开发

Phase 3 → Phase 4: 用户愿意和 AI 聊天？
  NO → 优化角色设定 / 记忆系统
  OK → 进入命书 + 付费开发

Phase 4 → Phase 5: 有用户愿意付费？
  NO → 调整定价 / 优化免费内容
  YES → 联调上线
```

---

## 风险管理

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| Claude API 响应慢 | 中 | 用户体验差 | 前端加载动画 + 后端超时重试 |
| 运势内容同质化 | 中 | 用户觉得无聊 | 基于用户画像逐步个性化 |
| 用户不愿付费 | 中 | 商业模型不成立 | 先用免费版验证价值，再调定价 |
| 合规风险 (命理类) | 低 | App 被下架 | 标注"娱乐参考"，避开医疗/金融 |
| 竞品快速跟进 | 高 | 失去先发优势 | 聚焦用户体验深度，不是功能多 |

---

## Phase 6: 管理后台 (已完成, v0.4)

### 目标
- 运营/管理员能通过后台管理系统查看和管理用户数据
- 独立认证体系，不依赖用户端 JWT
- 核心数据可视化（仪表盘趋势图）

### 架构决策

```
独立 Next.js 应用:
  └── 理由: 不与消费端共用构建配置/依赖
  └── 位置: /admin/ (端口 3002)
  └── 认证: 独立 Admin JWT (24h 过期)

共有后端路由:
  └── 前缀: /api/v1/admin/*
  └── 中间件: adminAuth (JWT + DB 双重验证)
  └── 安全: 独立的 ADMIN_JWT_SECRET 环境变量

数据层:
  └── User 模型新增 role / isDeleted / deletedAt 字段
  └── 软删除: 数据保留但标记删除，可恢复
  └── JSON 字段: String 存储 + JSON.parse 还原 (SQLite 兼容)
```

### 实现内容

#### 后端 (backend/src/)

| 文件 | 说明 |
|------|------|
| `routes/admin.ts` | 16 个 Admin API 端点 (认证/用户/运势/占卜/对话/仪表盘) |
| `middleware/adminAuth.ts` | 双重验证中间件 (JWT + DB role 检查) |
| `seed.ts` | 管理员账号创建脚本 |
| `config/index.ts` | 新增 adminJwt 配置项 |

#### 前端 (admin/src/)

| 文件 | 说明 |
|------|------|
| `app/login/page.tsx` | 管理员登录页 (邮箱+密码) |
| `app/dashboard/page.tsx` | 仪表盘 (统计卡片 + 趋势图 + 排行) |
| `app/users/page.tsx` | 用户列表 (搜索/筛选/排序/分页/删除恢复) |
| `app/users/[id]/page.tsx` | 用户详情编辑页 |
| `app/fortunes/page.tsx` | 运势列表 (按用户/日期/分数筛选) |
| `app/fortunes/[id]/page.tsx` | 运势详情 (含 JSON 数据查看) |
| `app/archives/page.tsx` | 占卜记录列表 (按类型/状态筛选) |
| `app/archives/[id]/page.tsx` | 占卜记录详情 (含 AI 解读/原始数据) |
| `app/chat-contexts/page.tsx` | 对话记录列表 |
| `app/chat-contexts/[id]/page.tsx` | 对话消息历史 (聊天气泡) |
| `components/auth-guard.tsx` | 路由守卫 (未登录重定向) |
| `components/sidebar.tsx` | 侧边导航栏 |
| `lib/api.ts` | Admin API 客户端 |
| `lib/auth-context.tsx` | 认证上下文 Provider |

### 管理员账号

```
邮箱: admin@starfate.app
密码: admin888
创建: npx tsx src/seed.ts
```

### 验证项

```
1. ✅ 管理员登录成功，获取 admin JWT
2. ✅ 用户列表/搜索/排序/分页正常
3. ✅ 用户编辑/软删除/恢复功能正常
4. ✅ 运势/占卜/对话列表和详情正常
5. ✅ 仪表盘统计数据和趋势图正确
6. ✅ 用户端 JWT 无法访问 admin 接口 (403)
7. ✅ 已删除管理员账号无法通过中间件验证
8. ✅ 前端路由守卫正确拦截未登录访问
```
