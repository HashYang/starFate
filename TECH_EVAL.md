# 技术评估文档：如何让 AI 命理更准确、更有依据

> 基于 PRD v0.4 | 2026-05-19 | Phase 0 完成 + 灵签 v1 上线 + 管理后台上线

---

## 核心命题

> 用户说"准" = AI 的输出让她觉得**说中了自己**、**有道理**、**有用**

这不是玄学，而是系统工程。本文从四个功能模块拆解如何实现"准"。

---

## 一、每日运势 — 从随机到有据可依

### 问题分析

```
当前常见做法:
  LLM 根据星座/生肖直接生成运势文案
  → 问题: 随机性大，同星座用户收到完全不同的内容
  → 结果: 一个用户说准，另一个说完全不对

根因:
  · 没有基于真实的星历/行运数据
  · 缺少用户画像个性化
  · 文案模板化，缺乏细节
```

### 方案：分层数据驱动

```
第一层: 真实星历数据 (Ephemeris Data)
  └── 用户出生星盘 + 当日行星位置 → 算出真实的相位关系
  └── 这是"准"的基础，不是 AI 编的

第二层: 用户画像个性化
  └── 用户的出生信息 → 本命盘
  └── 用户的占卜历史 → 当前关注点
  └── 用户反馈 → 偏好调整

第三层: AI 文案生成
  └── 基于第一层+第二层的数据 → AI 组织语言
  └── AI 不做预测，只做解读
```

### 技术实现

#### 数据源

| 数据 | 来源 | 说明 |
|------|------|------|
| 星历数据 | Swiss Ephemeris (免费) | 行星位置、相位、宫位 |
| 星座特质 | 知识库 | 12 星座性格特征 |
| 行运解读 | 知识库 | 行星行运的传统解读 |
| 用户画像 | 数据库 | 出生信息、历史行为 |

#### 星座运势生成流程

```
每日 00:00 批处理:

1. 获取当日行星位置 (Swiss Ephemeris)
   - 太阳、月亮、水星、金星、火星、木星、土星位置
   - 主要相位 (合相/冲相/三分相等)

2. 按星座计算
   - 太阳星座的行运影响
   - 上升星座的宫位影响
   - 月亮星座的情绪影响

3. 生成结构化数据 (JSON)
   {
     "constellation": "天蝎座",
     "date": "2026-05-18",
     "overall": { "score": 78, "influence": "月亮进入天秤座..." },
     "love": { "score": 82, "aspect": "金星三分木星...", "advice": "..." },
     "career": { "score": 65, "aspect": "水星冲土星...", "advice": "..." },
     "wealth": { "score": 70, ... },
     "health": { "score": 75, ... },
     "lucky": { "color": "深蓝", "number": 7, "direction": "东南" }
   }

4. AI 据结构化数据生成自然语言
   - 输入: 结构化数据 + 用户画像
   - 输出: 个性化运势文案
   - 控制: 温度 0.4 (低随机性，保持一致性)
```

#### 宜/忌动态生成 (Phase 0 已实现)

```
宜/忌不是 AI 随机生成，而是基于传统黄历的天干地支规则：

计算方法:
  1. 以 2000-01-01 (甲子日，地支=0) 为参考点
  2. 计算当天距离参考点的天数差额
  3. diff % 12 → 当日地支 (0=子, 1=丑, ..., 11=亥)
  4. 每个地支对应固定的宜/忌列表 (传统黄历规则)

个性化:
  根据用户出生时辰 + 生肖对宜/忌列表进行轮转偏移
  → 同一天不同用户看到的宜/忌顺序不同
  → 但同一用户每天看到的宜/忌内容是一致的

示例:
  today = 酉日 → yi = ["酉日宜 · 祭祀祈福", "嫁娶订婚", "修造动土"]
                 ji = ["酉日忌 · 修造动土", "求医治病", "争讼斗殴"]
```

#### 准确性保障

```
同星座用户差异化的方法:
  · 根据用户上升星座微调 (每个用户上升不同)
  · 根据用户关注点调整文案侧重 (感情/事业)
  · 参考用户历史反馈 (说"准"多的方面重点写)

质量监控:
  · A/B 测试不同 prompt 的"准"率
  · 用户反馈收集 ("准"/"不准" 按钮)
  · 每周分析低分案例持续优化
```

#### 灵签 — 八字五行分析 (v0.3 新增)

```
每日灵签不是随机模板，而是基于真实的天干地支计算 + 五行生克分析:

计算流程:
  1. 计算用户的日柱天干 (出生日的天干，即"日主")
     └── 以 2000-01-01 (甲子日) 为参考点
     └── 计算目标日期距离参考点的天数差额
     └── 天干: diff % 10 → 0=甲, 1=乙, ..., 9=癸

  2. 计算今天的日柱 (天干+地支)
     └── 同上算法，diff % 10 得天干，diff % 12 得地支
     └── 例如 戊戌日 → stem=4 (戊), branch=10 (戌)

  3. 五行映射
     ┌────────────┬──────────────────────────────┐
     │ 天干        │ 五行属性                       │
     ├────────────┼──────────────────────────────┤
     │ 甲/乙       │ 木 (Wood)                     │
     │ 丙/丁       │ 火 (Fire)                     │
     │ 戊/己       │ 土 (Earth)                    │
     │ 庚/辛       │ 金 (Metal)                    │
     │ 壬/癸       │ 水 (Water)                    │
     └────────────┴──────────────────────────────┘

  4. 五行生克关系判断
     ┌──────────────┬──────────────────────────────┐
     │ 关系         │ 含义                         │
     ├──────────────┼──────────────────────────────┤
     │ same         │ 日主五行 = 今日天干五行        │
     │ generates_me │ 今日五行生日主五行 (生我)      │
     │ i_generate   │ 日主五行生今日五行 (我生)      │
     │ controls_me  │ 今日五行克日主五行 (克我)      │
     │ i_control    │ 日主五行克今日五行 (我克)      │
     └──────────────┴──────────────────────────────┘
     生克规则: 木→火→土→金→水→木 (相生)
               木→土→水→火→金→木 (相克)

  5. 生成个性化签语
     └── 根据关系选择对应的签语模板 (每种关系 2 个变体)
     └── 签语 = 四字短语 + 七言诗句风格
     └── 基调色 (baseTone) = 对应五行色

  6. 四段命理解读 (40 个模板变体)
     └── 事业/财运/感情/健康四个维度
     └── 每个维度 5 种关系 × 2 变体 = 10 种解读

  7. 行动指引 + 吉时推荐
     └── 时辰推荐算法: 根据五行相生相克选择最有利的时辰

准确率保障:
  · 所有计算基于历法算法，无随机性
  · 同一用户同一天 → 完全相同的结果
  · 不同用户因日主不同 → 看到不同的签语和解读
  · 数据来源: 中国传统命理规则 (子平术)

示例:
  用户日主 = 癸 (水), 今日 = 戊戌 (土)
  → 关系: controls_me (土克水)
  → 签语: "厚土载物 · 守静笃"
  → 事业: 压力即动力
  → 推荐时辰: 申时/酉时 (金生水，化解土克水)
```

---

## 二、AI 占卜 — 从模板到专业解读

### 问题分析

```
常见做法:
  LLM 随机抽牌 + 即时生成解读
  → 问题: 牌意解读可能偏离传统释义
  → 问题: 用户问题不同但回复结构雷同
  → 问题: 没有考虑牌与牌之间的互动
```

### 方案：知识库 + 结构化解读

#### 塔罗知识库构建

```
知识库内容:
  ├── 78 张牌的标准释义 (韦特体系)
  ├── 每张牌的正位/逆位含义
  ├── 每张牌在感情/事业/财运场景的侧重
  ├── 常用牌阵及位置含义
  ├── 牌与牌之间的互动关系
  └── 经典牌例解读 (few-shot examples)

存储方式:
  ├── 核心知识 → prompt system message (固定)
  ├── 详细释义 → RAG 向量库 (按需检索)
  └── 用户上下文 → session context (动态)
```

#### 占卜流程 (RAG + Prompt)

```
用户提问: "他对我什么感觉？"

Step 1: 问题分析
  ├── 提取主题: 感情
  ├── 提取对象: "他"
  ├── 提取情绪: 不确定/焦虑
  └── 提取关键信息: 暗恋/暧昧

Step 2: 抽牌 (根据牌阵)
  ├── 三牌: 过去-现在-未来
  ├── 从 78 张中随机抽取
  ├── 30% 概率逆位
  └── 确保不重复

Step 3: 检索相关知识
  ├── 从向量库检索三张牌的详细释义
  ├── 检索感情场景的侧重解读
  └── 检索牌与牌之间的组合解读

Step 4: AI 生成解读
  输入:
    ├── 用户问题 + 主题分类
    ├── 三张牌的具体信息 + 检索到的释义
    ├── 牌阵位置含义
    ├── 用户画像 (星座/历史)
    └── 标准解读框架

  控制:
    ├── 温度 0.5-0.6 (专业但不过于随机)
    ├── 强制输出结构: 总结 → 逐牌分析 → 综合建议
    └── 长度: 简版 100-150 字 / 详版 300-500 字
```

#### 简版 vs 详版

```
简版 (免费):
  结构: 核心结论 (3句) + 关键牌提示
  字数: 80-120 字
  特点: 快速给出方向，引导付费

详版 (付费):
  结构: 问题分析 → 每张牌逐张解读 → 牌面组合分析
        → 综合建议 → 未来关注点
  字数: 300-500 字
  特点: 深度、具体、可执行
```

#### 准确性增强方案

```
1. RAG 检索增强
   · 将塔罗经典书籍/资料向量化
   · 每次解读检索最相关的 3-5 条参考资料
   · 减少 AI 胡编乱造

2. 牌面组合逻辑
   · 预定义牌与牌之间的互动规则
   · 例如: 恋人+恶魔 = 纠缠关系
   · 例如: 星星+月亮 = 希望中的不安

3. 反馈闭环
   · 用户可标记"准"/"不准"
   · 低分案例人工review后优化 prompt
   · 每周更新 few-shot examples
```

---

## 三、AI 对话 — 从泛泛而谈到知心陪伴

### 问题分析

```
对"准"的需求在这里不同:
  用户要的不是预测，而是"被理解"和"持续关注"

关键挑战:
  · AI 要记住之前的对话 (长期记忆)
  · AI 要保持一致的个性 (角色一致性)
  · AI 要能关联之前的占卜记录
  · AI 要在免费版提供足够价值，引导付费
```

### 方案：分级记忆系统

#### 记忆分层

```
记忆分三层，成本从低到高:

第一层: Session 上下文 (当前对话)
  └── 存储: 内存 + Redis
  └── 保留: 当前会话的最近 20 轮
  └── 成本: 低 (在 prompt 内)
  └── 作用: 对话连贯性

第二层: 用户画像 (持久)
  └── 存储: PostgreSQL
  └── 包含: 出生信息、星座、关注领域
  └── 成本: 低 (每次请求加载)
  └── 作用: 个性化基础

第三层: 命理历史 (持久 + 摘要)
  └── 存储: PostgreSQL + 向量库
  └── 包含: 过往占卜记录、用户反馈
  └── 成本: 中 (按需检索)
  └── 作用: 关联历史，"你还记得上次..."
```

#### 对话流程

```
用户: "他三天没找我了"

Step 1: 加载上下文
  ├── 用户画像 (星座/感情状态)
  ├── 最近 3 次占卜记录 (如果有)
  └── 当前 session 最近 10 轮对话

Step 2: 构建 prompt
  ├── System: AI 命理师角色设定
  ├── Context: 用户画像 + 历史摘要
  ├── Few-shot: 感情场景的对话样例
  └── User: 当前问题

Step 3: AI 回复
  免费版: 2-3 句共情回应
  付费版: 完整分析 + 占卜引导

Step 4: 存储
  ├── 当前轮对话存入 session
  ├── 提取关键词存入用户画像
  └── 生成对话摘要 (定期)
```

#### 角色一致性

```
命理师人设:
  名字: "星命" (暂定)
  性格: 温柔但有智慧，神秘但不故弄玄虚
  语气: 中文为主，偶尔用占卜术语
  原则: 给予希望但不虚假，给出具体建议

技术保障:
  · System prompt 详细定义人设 (200+ 字)
  · Few-shot examples 展示语气和风格
  · Temperature 0.5-0.6 (保持风格稳定)
  · Response format 约束 (结构化的 JSON)
```

#### 免费→付费转化点

```
免费版回复结构:
  1-2 句共情 (建立连接)
  1 句基于知识的分析 (展示价值)
  1 句引导 ("想听听塔罗对这个情况的看法吗？")

付费版回复结构:
  3-5 句深度分析
  关联历史占卜
  具体行动建议
  后续关注点
```

---

## 四、命书 — 从一次性到持续价值

### 问题分析

```
命书的核心矛盾:
  用户愿意付费是因为"深度"和"个性化"
  但深度报告是静态的，用户看完就结束了

解决思路:
  命书不是一次性报告，而是持续更新的个人命运档案
  免费试读展示价值 → 付费解锁完整报告
  购买后有月度更新 → 持续回访理由
```

### 方案：模板 + 个性化 + AI

#### 命书生成流程

```
Step 1: 用户数据收集
  ├── 出生日期/时间/地点
  ├── 星座/上升星座/月亮星座计算
  ├── 八字排盘 (年柱/月柱/日柱/时柱)
  └── 紫微斗数基础 (可选 v2)

Step 2: 结构化报告生成

  模块 A: 性格特质 (免费试读)
    ├── 基于星座+八字的核心性格
    ├── 3-5 条关键特质，每条 2-3 句
    └── 免费展示，吸引付费

  模块 B: 感情模式 (付费)
    ├── 感情风格描述
    ├── 适配类型分析
    ├── 容易遇到的问题
    └── 建议

  模块 C: 事业发展 (付费)
    ├── 天赋优势领域
    ├── 适合的职场角色
    ├── 关键发展时期
    └── 建议

  模块 D: 财运趋势 (付费)
    ├── 财运风格
    ├── 适宜投资类型
    ├── 关键时间节点
    └── 建议

Step 3: AI 生成
  ├── 输入: 结构化数据 + 模块模板
  ├── 控制: 温度 0.3 (保持专业性和一致性)
  ├── 输出: 每个模块 200-400 字
  └── 总长度: 免费 200 字 / 付费 1500-2000 字
```

#### 命书更新机制

```
购买命书后:
  ├── 每月 1 号生成月度运势更新
  ├── 基于当月行运 + 用户本命盘
  ├── 推送通知: "你的命书更新了"
  └── 打开即可查看

持续价值:
  用户每月回来查看更新 = 持续付费意愿
  "买了命书不是结束，是服务的开始"
```

---

## 五、系统架构总览

### 技术栈 (Phase 0 实际架构)

```
当前 MVP 是 Web 优先，iOS 后续

┌─────────────────────────────────────────────┐
│            Web App (Next.js 16)               │
│  React 19 + Tailwind v4 + shadcn/ui           │
│  Pages: / /login /divination /chat            │
│  /daily-sign /fate-book /profile              │
│  Zen theme: bg #f5f0e8, surface #fefcf9        │
│  Primary: #c4835a, glassmorphism cards          │
└──────────────────┬──────────────────────────┘
                   │ HTTPS (localhost:3000 ↔ 3001)
┌──────────────────▼──────────────────────────┐
│         API Gateway (Express + tsx)           │
│  Routes: /api/v1/auth /readings /chat /user   │
│  Port: 3001 (dev)                             │
│  Middleware: auth (JWT) / rateLimit / logging │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│              核心服务层                         │
│                                              │
│  ┌──────────────┐  ┌──────────────────┐      │
│  │  用户服务     │  │  运势服务         │      │
│  │  · 注册/登录  │  │  · 当日地支计算  │      │
│  │  · 八字信息   │  │  · 宜忌对照表   │      │
│  │  · 星座/生肖  │  │  · 本地兜底算法  │      │
│  └──────────────┘  └──────────────────┘      │
│                                              │
│  ┌──────────────┐  ┌──────────────────┐      │
│  │  占卜服务     │  │  AI 对话服务     │      │
│  │  · 抽牌算法   │  │  · 上下文记忆   │      │
│  │  · Claude AI  │  │  · Claude AI    │      │
│  │  · 解读生成   │  │  · 角色扮演     │      │
│  └──────────────┘  └──────────────────┘      │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│              数据层                             │
│                                              │
│  ┌──────────────────┐                        │
│  │ SQLite (dev.db)   │  ← 开发用 SQLite       │
│  │ · 用户数据        │    生产切 PostgreSQL    │
│  │ · 占卜历史        │                        │
│  │ · 运势/档案       │                        │
│  │ · JSON 字段存为   │                        │
│  │   String + JS解析 │                        │
│  └──────────────────┘                        │
└─────────────────────────────────────────────┘

外部服务:
  ┌────────────┐
  │ Claude API │  ← 无 API Key 时自动用本地算法兜底
  │ · 文本生成  │
  │ · 对话     │
  └────────────┘

注: 开发时无 API Key/PostgreSQL 也能完整运行
  · 运势: 本地算法基于当日地支+用户生辰生成
  · 数据库: SQLite 零配置
  · 注册/登录: JWT + localStorage
```

### Prompt 体系总览

```
┌─────────────────────────────────────────┐
│          Prompt 工程体系                    │
├─────────────────────────────────────────┤
│                                         │
│  System Prompts (固定，每个请求加载)       │
│  ├── 命理师角色设定 (300字)               │
│  └── 输出格式约束 (JSON schema)           │
│                                         │
│  Knowledge Base (RAG，按需检索)            │
│  ├── 塔罗牌详细释义 (每张 200+ 字)         │
│  ├── 星座特质库 (12星座，每星座 500+ 字)    │
│  ├── 牌阵规则 (卡组/牌阵 50+ 条)           │
│  └── 经典案例 (few-shot，持续积累)          │
│                                         │
│  Context (动态，每次不同)                  │
│  ├── 用户画像摘要 (持久数据摘要)            │
│  ├── 历史对话摘要 (最近 N 次对话)           │
│  ├── 当前问题/占卜内容                     │
│  └── UI 状态 (是否需要引导、转化提示)        │
│                                         │
│  Prompt Chain (多步处理)                   │
│  ├── Step 1: 问题分类/意图识别             │
│  ├── Step 2: 数据检索/补全                │
│  └── Step 3: 最终生成                     │
└─────────────────────────────────────────┘
```

---

## 六、为什么这样设计能更"准"

### 对比总结

| 维度 | 简单做法 (多数竞品) | 我们的做法 |
|------|-------------------|-----------|
| 运势 | LLM 直接生成 | 星历数据+用户画像→结构化→AI润色 |
| 占卜 | AI 随机抽牌+即时解读 | 知识库检索+标准化流程+RAG |
| 对话 | 无记忆 | 三级记忆系统+角色一致性 |
| 命书 | 一次性报告 | 结构化模块+月度更新 |
| 质量 | 无反馈机制 | 用户反馈闭环+持续优化 |

### 关键原则

```
1. AI 不做预测，只做解读
   · 行星位置是真实数据
   · 塔罗牌意是既定知识
   · AI 的职责是组合这些信息，输出有温度的语言

2. 结构化保证一致性
   · 输入结构化 (分字段)
   · 输出结构化 (JSON)
   · 流程结构化 (多步 chain)

3. 反馈驱动优化
   · 每个功能都有"准/不准"反馈
   · 低分案例自动标记
   · 定期分析优化 prompt
```

---

## 七、开发优先级

| 阶段 | 内容 | 周期 |
|------|------|------|
| Phase 0 | Prompt 工程 + 知识库搭建 | 1-2 周 |
| Phase 1 | 每日运势 (星历接入+AI 生成) | 2 周 |
| Phase 2 | AI 占卜 (RAG+结构化解读) | 2 周 |
| Phase 3 | AI 对话 (记忆系统+角色人设) | 2 周 |
| Phase 4 | 命书 (报告生成+更新机制) | 2 周 |
| Phase 5 | iOS UI 对接 + 整体联调 | 2 周 |
| Phase 6 | 用户反馈系统 + 质量优化 | 持续 |

---

## 八、管理后台架构 (v0.4 新增)

### 8.1 架构总览

管理后台是一个与用户端完全隔离的独立应用，共享同一后端服务，但使用独立的认证体系和路由命名空间。

```
┌─────────────────────────────────────┐
│    Admin Web App (Next.js 16)        │
│    Port: 3002                        │
│    Routes: /login /dashboard         │
│            /users /fortunes          │
│            /archives /chat-contexts   │
│    Auth: Admin JWT (localStorage)    │
└──────────┬──────────────────────────┘
           │ HTTPS (localhost:3002 ↔ 3001)
┌──────────▼──────────────────────────┐
│    API Gateway (Express)              │
│    Route: /api/v1/admin/*            │
│    Middleware: adminAuth (双重验证)    │
└──────────┬──────────────────────────┘
           │
┌──────────▼──────────────────────────┐
│    Data Layer (Prisma + SQLite)      │
│    User 新增字段:                    │
│      role        String  @default("user")
│      isDeleted   Boolean @default(false)
│      deletedAt   DateTime?
└─────────────────────────────────────┘
```

### 8.2 安全架构

#### 独立 JWT 机制

```
用户 JWT                        Admin JWT
┌──────────────────┐           ┌──────────────────┐
│ secret: JWT_SECRET│           │ secret: ADMIN_   │
│                   │           │        JWT_SECRET │
│ expiresIn: 7d    │           │ expiresIn: 24h   │
│ payload: {userId, │           │ payload: {userId, │
│          role}    │           │          role}    │
└──────────────────┘           └──────────────────┘
```

优势:
- 用户 token 泄露不影响管理后台
- Admin token 24h 短过期降低风险
- 可独立轮换密钥

#### 双重验证中间件

```typescript
// adminAuth.ts 核心逻辑
// 第一关: JWT 验证 → 获取 payload 中的 userId + role
// 第二关: 数据库实时查询 → 确认用户 role === 'admin' && isDeleted === false
```

#### 前端安全

- `AuthGuard` 路由守卫: 所有页面 (除 `/login`) 都需要有效 token
- `401/403` 自动处理: API 客户端检测到未授权时清除 token 并重定向到登录页
- 侧边栏只对已认证管理员可见

### 8.3 API 设计模式

#### 统一响应格式

```typescript
// 成功
{ "data": { ... }, "total": 100, "page": 1, "hasMore": true }

// 错误
{ "error": { "message": "..." } }
```

#### 分页规范

```
参数: page (默认 1), pageSize (默认 20)
响应: { items: [...], total: number, page: number, pageSize: number, hasMore: boolean }
```

#### JSON 字段处理

所有 JSON 字段 (如 `cards`, `readingResult`, `messages`, `divineSign`) 在 SQLite 中存为 String，API 返回前 `JSON.parse` 还原为对象。

```typescript
function parseJsonField<T>(field: string | null | undefined): T | null {
  if (!field) return null;
  try { return JSON.parse(field); } catch { return null; }
}
```

### 8.4 仪表盘统计设计

```sql
-- 聚合统计为 4 个核心指标
总用户数       → SELECT COUNT(*) FROM User WHERE isDeleted = false
今日活跃用户   → SELECT COUNT(*) FROM ... WHERE lastActiveAt >= today
总占卜数       → SELECT COUNT(*) FROM FateArchive
预测准确率     → fulfilled / (fulfilled + unfulfilled)
```

趋势数据按天聚合，返回最近 N 天 (默认 30 天) 的每日计数，前端用 Chart.js 柱状图展示。

### 8.5 数据库变更

User 模型新增 3 个字段:

```prisma
role       String    @default("user")     // "user" | "admin"
isDeleted  Boolean   @default(false)      // 软删除标记
deletedAt  DateTime?                      // 删除时间
```

设计考量:
- 用 `isDeleted` 而非物理删除: 保留用户数据的完整性和关联查询
- 用 `deletedAt` 记录时间: 可追踪删除操作，支持自动清理过期数据
- `role` 默认 "user": 大部分用户不受影响，管理员手动创建/升级

### 8.6 前端组件体系

```
admin/src/components/
├── auth-guard.tsx       # 路由守卫: 未登录重定向
├── sidebar.tsx          # 侧边导航栏 (5 项 + 退出按钮)
├── stats-card.tsx       # 统计卡片 (标题/数值/描述)
├── data-table.tsx       # 通用分页表格 (loading/empty/error)
├── delete-dialog.tsx    # 删除确认弹窗
└── json-viewer.tsx      # JSON 数据只读查看器
```

### 8.7 部署说明

| 组件 | 方式 | 说明 |
|------|------|------|
| Admin 前端 | Vercel (独立项目) | 与用户端分离部署，独立域名 |
| Admin API | 复用现有后端 | 现有 Express 服务，新增路由前缀 |
| 管理域名 | admin.starfate.app | 可与主站不同域名避免混淆 |

---

## 九、开放问题 (待决策)

### 技术选型

1. **数据库 — SQLite vs PostgreSQL**
   - 开发阶段用 SQLite (零配置，文件存储)，生产环境切 PostgreSQL
   - 注意: SQLite 不支持原生 Json 和 String[] 类型
   - 当前处理: 所有 Json/String[] 字段 => String，手动 JSON.stringify/JSON.parse
   - 后续切换到 PostgreSQL 时只需改 schema 中 provider 和移除 JSON.parse 调用

2. **星历数据** — 使用 Swiss Ephemeris (C 库，需要编译) 还是调用第三方 API？
   - Swiss Eph: 免费、精确、但需要服务器端集成
   - API (如 astro.com): 方便但有调用成本

3. **向量库** — PGVector (用现有 PostgreSQL) 还是独立部署 Pinecone/Milvus？
   - PGVector: 简单，不需要额外基础设施
   - 独立向量库: 性能好但复杂度高

4. **缓存策略** — 每日运势是每日批处理生成还是实时生成？
   - 批处理: 一致性高，成本低，但缺少实时个性化
   - 实时: 可个性化，但成本高，波动大

### 知识库构建

4. **塔罗知识库来源** — 是否需要购买/整理权威的塔罗资料库？还是基于公开资料构建？

5. **八字/紫微模块** — v1 是否加入简单的八字排盘？还是纯塔罗+星座起步？
